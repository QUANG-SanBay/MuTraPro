# Sample RabbitMQ config (keep defaults for dev)
# listeners.tcp.default = 5672
# management.tcp.port = 15672
# loopback_users.guest = true
# vm_memory_high_watermark.relative = 0.5
# queue_master_locator = min-masters
